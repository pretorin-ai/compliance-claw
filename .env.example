# Copy to .env and fill in. .env is gitignored and is excluded from the Docker
# build context by .dockerignore, so these never end up baked into an image layer.

PRETORIN_API_KEY=
OPENCLAW_GATEWAY_TOKEN=
